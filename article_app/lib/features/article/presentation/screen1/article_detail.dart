import 'package:flutter/material.dart';

class Article_detail extends StatelessWidget {
  const Article_detail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("data"),
    );
  }
}
